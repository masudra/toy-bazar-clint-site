import { useLoaderData } from "react-router-dom";

const EditMyInfo = () => {
    const data  =useLoaderData();
    // const { _id, Price, sellerName, toyName, Sub_category, Available_quantity } =data
    // console.log(data)
    return (
     <div>
        
     </div>
    );
};

export default EditMyInfo;